{
  "name": "sidemount-authority",
  "version": "1.0.0",
  "description": "Sidemount Authority — sidemount scuba diving training & gear reviews",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "dev": "eleventy --serve --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-plugin-rss": "^1.2.0",
    "@11ty/eleventy-navigation": "^0.3.5"
  }
}
